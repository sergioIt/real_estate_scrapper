## Main descripption

ss.ge web parser requirements separated by stages

1. just fetch last 10 uniq records from site and provide as a list to std output as a result
2. use n as param for the last n uniq records and  provide as a list to std output as a result



### MVP version:
- telegram bot integration
- user initiates new search with criteria (only price range)
- then bot sends last 10 new non-duplicate records matching criteria daily at certain time (e.g., 9am)
- user can remove this search/subscription